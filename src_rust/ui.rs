// src_rust/ui.rs
// UI теперь через Tauri команды, а не через веб-сервер
// Этот модуль оставлен для совместимости, но не используется

pub struct UIController;

impl UIController {
    pub fn new() -> Self {
        Self
    }
}